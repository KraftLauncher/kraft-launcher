abstract final class TestConstants {
  /// An indicator that this String value is irrelevant.
  static const anyString = 'any';

  /// An indicator that this int value is irrelevant.
  static const anyInt = 1;

  /// An indicator that this bool value is irrelevant.
  static const anyBool = false;
}
